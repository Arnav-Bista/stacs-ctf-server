import { FlagSubmission } from "./flag-submission";

export default function FlagSubmissionPage() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <FlagSubmission className="p-8 text-lg w-full max-w-screen-sm" />
    </div>
  );
}
